#include <stdio.h>

int main(void) {
  // declara 3 variáveis
  float n1, n2, res;
  // atribui valores às variáveis
  n1 = 6;
  n2 = 8;
  // atribui o produto de 2 variáveis à uma terceira
  res = (n1 + n2) / 2;
  // joga na tela o valor da variável
  printf("%.2f", res);
  return 0;
}